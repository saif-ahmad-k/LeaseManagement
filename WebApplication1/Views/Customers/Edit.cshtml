@model WebApplication1.Models.tblCustomer

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="content container-fluid">

        <!-- Page Header -->
        <!--<div class="crms-title row bg-white mb-4">
            <div class="col">
                <h3 class="page-title">
                    <span class="page-title-icon bg-gradient-primary text-white mr-2">
                        <i class="la la-table"></i>
                    </span> <span>Attendance</span>
                </h3>
            </div>-->
        @*<div class="col text-right">
                <ul class="breadcrumb bg-white float-right m-0 pl-0 pr-0">
                    <li class="breadcrumb-item"><a href="#">Home</a></li>
                    <li class="breadcrumb-item active">Dashboard</li>
                </ul>
            </div>*@
        <!--</div>-->
        <!-- /Page Header -->
        <div class="card mb-4 mt-3" style="padding-top:10px">
            <div class="card-header">
                <h5 class="card-title mb-0">Update</h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-sm">
                        <form class="needs-validation" novalidate>
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            @Html.HiddenFor(p => p.ID)
                           
                            <div class="form-group">
                                @Html.Label("Last Name", htmlAttributes: new { @class = "control-label col-md-2" })
                                <div class="col-md-12 mb-3">
                                    @Html.EditorFor(p => p.LastName, new { htmlAttributes = new { @class = "form-control", autocomplete = "off" } })
                                </div>
                            </div>
                            
                            <br />

                            <div class=" row pull-right">
                                <button type="submit" id="btnSave" class="btn btn-primary" style="padding-right:15px">Save</button>
                                <div class="mt-0 btn btn-warning">@Html.ActionLink("Back to List", "Index")</div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
}